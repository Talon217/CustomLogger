[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "custom_logger"
version = "0.1.0"
description = "Custom ANSI color logging formatter with rotating file handlers"
readme = "README.md"
authors = [{ name = "Blaine Horner" }]
requires-python = ">=3.10"
dependencies = []